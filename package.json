{
  "name": "ivnbieb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "covers": "node scripts/fetch_covers.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.1",
    "msw": "^2.12.14",
    "vitest": "^4.1.1"
  }
}
